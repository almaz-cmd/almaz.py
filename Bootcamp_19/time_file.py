# from datetime import datetime
#
# now = datetime.now()
# b_d = datetime(2024, 1, 16)
# p = b_d - now
#
#
# years = p.days // 365
# days = p.days % 365
# seconds = p.seconds
# microseconds = p.microseconds
#
# minutes = seconds // 60
# seconds = seconds % 60
#
# print("{} лет {} дней {} минут {} секунд {} микросекунд".format(years, days, minutes, seconds, microseconds))
